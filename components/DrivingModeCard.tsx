import { CarFront, ChevronRight, Radio } from 'lucide-react-native';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors, shadow } from '@/lib/theme';

export function DrivingModeCard({ active, onToggle }: { active: boolean; onToggle: () => void }) {
  return <View style={[styles.card, shadow, active && styles.active]}><View style={[styles.icon, active && styles.iconActive]}><CarFront size={22} color={active ? '#fff' : colors.teal} /></View><View style={styles.copy}><View style={styles.titleRow}><Text style={styles.title}>Driving mode</Text><View style={[styles.pill, active && styles.pillActive]}><Radio size={11} color={active ? colors.green : colors.muted} /><Text style={[styles.pillText, active && { color: colors.green }]}>{active ? 'LIVE' : 'READY'}</Text></View></View><Text style={styles.body}>{active ? 'Monitoring your route for hazards in the background.' : 'Automatically detect when you start driving.'}</Text></View><Pressable onPress={onToggle} style={styles.arrow}><ChevronRight size={19} color={colors.muted} /></Pressable></View>;
}
const styles = StyleSheet.create({ card: { backgroundColor: colors.surface, borderRadius: 20, padding: 15, flexDirection: 'row', alignItems: 'center', gap: 12, marginBottom: 18 }, active: { borderWidth: 1, borderColor: colors.green }, icon: { width: 44, height: 44, borderRadius: 14, backgroundColor: colors.tealSoft, alignItems: 'center', justifyContent: 'center' }, iconActive: { backgroundColor: colors.green }, copy: { flex: 1 }, titleRow: { flexDirection: 'row', alignItems: 'center', gap: 9 }, title: { color: colors.ink, fontSize: 15, fontWeight: '800' }, pill: { flexDirection: 'row', alignItems: 'center', gap: 4, paddingHorizontal: 7, paddingVertical: 4, borderRadius: 8, backgroundColor: colors.canvas }, pillActive: { backgroundColor: colors.greenSoft }, pillText: { fontSize: 9, color: colors.muted, fontWeight: '900', letterSpacing: .7 }, body: { color: colors.muted, fontSize: 12, lineHeight: 17, marginTop: 4 }, arrow: { padding: 5 } });
