import { Activity, CheckCircle2, MapPinned } from 'lucide-react-native';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors } from '@/lib/theme';

export function PermissionCard({ granted, onGrant }: { granted: boolean; onGrant: () => void }) { return <View style={styles.card}><View style={styles.icon}><MapPinned size={20} color={colors.teal} /></View><View style={styles.copy}><Text style={styles.title}>Safety permissions</Text><Text style={styles.body}>{granted ? 'Location and activity access are enabled.' : 'Enable location and motion access for automatic hazard alerts.'}</Text></View>{granted ? <CheckCircle2 size={22} color={colors.green} /> : <Pressable onPress={onGrant} style={styles.button}><Activity size={14} color="#fff" /><Text style={styles.buttonText}>Enable</Text></Pressable>}</View>; }
const styles = StyleSheet.create({ card: { backgroundColor: colors.tealSoft, borderRadius: 18, padding: 15, flexDirection: 'row', alignItems: 'center', gap: 12, marginBottom: 20 }, icon: { width: 39, height: 39, borderRadius: 13, backgroundColor: '#fff', justifyContent: 'center', alignItems: 'center' }, copy: { flex: 1 }, title: { color: colors.ink, fontWeight: '800', fontSize: 14 }, body: { color: colors.muted, fontSize: 11, lineHeight: 16, marginTop: 3 }, button: { backgroundColor: colors.teal, borderRadius: 10, paddingHorizontal: 11, paddingVertical: 9, flexDirection: 'row', alignItems: 'center', gap: 5 }, buttonText: { color: '#fff', fontSize: 11, fontWeight: '800' } });
